# Configuration du port de l'application
server.port=8080

# Configuration PostgreSQL
spring.datasource.url=jdbc:postgresql://postgres-db:5432/authapi
spring.datasource.username=postgres
spring.datasource.password=monmotdepasse
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Hibernate) configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Configuration des sessions et tokens JWT (si utilisé)
app.jwt.secret=MonSecretSuperSecurise123456
app.jwt.expiration=3600000  # 1 heure en millisecondes
